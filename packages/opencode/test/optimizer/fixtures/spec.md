# Repository Specification — demo

## Repository overview
[FACT] The package defines `add` in calc.py:1 and subtract in calc.py:5.

## Technology stack
Python. See calc.py:1-2 for the add body, and calc.py:1,5 for both defs.

## Core modules
The symbol calc.py:subtract is defined; also calc.py:add:1 with a line.
A bad ref: calc.py:99 and missing one ghost.py:3.

**[fact]** an unsupported claim with no citation right here at all.

## Evidence index
- calc.py:add — the adder.
